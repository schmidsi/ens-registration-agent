# Network: mainnet or sepolia
NETWORK=sepolia

# RPC URL for the selected network
RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# Private key for registration transactions (required for registerName)
# Only needed if using the registration feature
PRIVATE_KEY=0x...

# x402 payment config (for HTTP API)
PAYMENT_ADDRESS=0x...  # Your wallet to receive USDC payments
PAYMENT_NETWORK=eip155:84532  # Base Sepolia for testing, eip155:1 for Ethereum Mainnet

# x402 test script (scripts/test-x402.ts) - Optional
# Falls back to PRIVATE_KEY if not set
# Needs Base Sepolia USDC - get from https://faucet.circle.com/
# PAYER_PRIVATE_KEY=0x...
